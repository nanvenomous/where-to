# https://taskfile.dev

version: '3'

tasks:
  default:
    desc: "build binary"
    cmds:
      - go build -o ./nav ./main.go 
      - echo 'run `eval "$(./nav)"` in your terminal to complete setup'
    silent: true
